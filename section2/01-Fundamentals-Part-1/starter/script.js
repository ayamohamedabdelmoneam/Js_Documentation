/*
const  country = "Egypt";
const  continent = "African";
let population = "8Millions";
console.log(country);
console.log(continent);
console.log(population);
let isIsland = true;

const language = "Arabic";
console.log(typeof(isIsland));
console.log(typeof(country));
console.log(typeof(language));

// Simple Use Case To Calculate The Age 
const now = 2023;
const ageAya = now - 2000;
const ageMamy = now - 1983;
console.log(ageAya, ageMamy ); // 23 40

// You can Make arithematic operation on Variable like This 
console.log(ageAya * 2); // 46
console.log(ageAya / 2); // 11.5
console.log(ageAya + 2); // 25
console.log(ageAya % 2); // 1
 
const markMass = 95;
const markHiegh = 1.88;
const markBMI = markMass / markHiegh ** 2;
const johnMass = 85;
const johnHigh = 1.76;
const johnBMI = johnMass / johnHigh ** 2;
if (markBMI > johnBMI) {
    console.log(`Mark's BMI (${markBMI} is higher than John's BMI (${johnBMI}))`);
}else {
    console.log(`John's BMI (${johnBMI}) is higher than Mark's BMI (${markBMI})`);
}

// Simple program To Build A String 
// I'm Aya, a 23 Years Old Front-End developer !
const firstName = "Aya";
const job = "Front_End developer";
const birthYear = 2000;
const year = 2023;

const aya = "I'm" +" "+  firstName + ", a" + (year - birthYear) + "Year Old" + job + "!";
console.log(aya);

const newAya = `I'm ${firstName}, a ${year - birthYear} Years Old ${job}!`
console.log(newAya);

const age = 15;
const isOldEnough = age >= 18;
if(age >= 18) {
    console.log(`Aya Can Start To Drive 🚗`);
}else {
    const yearsLeft = 18 -age
    console.log(`aya is too young. Wait another ${yearsLeft} years`);
}


// type conversion
const inputYear = '1991';
console.log(Number(inputYear), inputYear);
console.log(Number(inputYear) + 18);

console.log(Number('Jonas'));
console.log(typeof NaN);

console.log(String(23), 23);



// Equality Operators: == vs. ===
const age = "18";
if (age === 18) console.log('You just became an adult :D (strict)');

if (age == 18) console.log('You just became an adult :D (loose)');

const favourite = Number(prompt("What's your favourite number?"));
console.log(favourite);
console.log(typeof favourite);

if (favourite === 23) { // 22 === 23 -> FALSE
  console.log('Cool! 23 is an amzaing number!')
} else if (favourite === 7) {
  console.log('7 is also a cool number')
} else if (favourite === 9) {
  console.log('9 is also a cool number')
} else {
  console.log('Number is not 23 or 7 or 9')
}

if (favourite !== 23) console.log('Why not 23?');


const dolphinScore = (96 + 108 + 89) / 3;
const KoalasScoreAve = (88 + 91 + 110) /3 ;
if (dolphinScore > KoalasScoreAve) {
  console.log(`Dolphin Team Score Is ${dolphinScore} There Are Winners `);
}else if (dolphinScore === KoalasScoreAve){
  console.log(`The Two Team Are draw`);
}else{
  console.log(`Koalas Team Scor Is ${KoalasScoreAve} There Are Winners`);
}

//Bouns 1
const dolphinScore = (97 + 112 + 80) / 3;
const KoalasScoreAve = (106 + 95 + 10) /3 ;
console.log(dolphinScore, KoalasScoreAve);
if (dolphinScore > KoalasScoreAve && dolphinScore >= 100) {
  console.log(`Dolphin Winner!`);
}else if (KoalasScoreAve > dolphinScore && KoalasScoreAve >= 100) {
  console.log(`koalas Are Winners!`)
}else if (dolphinScore === KoalasScoreAve && dolphinScore >= 100 && KoalasScoreAve >= 100) 
{

  console.log(`Both Of Team Win!`);
}else{
  console.log(`No One Of Them Wins The Trophy 😢`);
}
*/


